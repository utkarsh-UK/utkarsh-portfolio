import React from "react";

import { SectionTitle } from "../../components/index";

// import classes from "./Traits.module.css";

const Traits = () => {
  return (
    <section id="traits">
      <SectionTitle
        mainTitle="My Traits"
        subTitle="What I Possess"
        backgroundTitle="WHY ME"
      />
    </section>
  );
};

export default Traits;
